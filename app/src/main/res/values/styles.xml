<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <!-- <style name="WhiteButton" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/white_button_bgd</item>
        <item name="android:textColor">@color/primary_color</item>
    </style> -->
    <!-- <style name="Button.GreenButton" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/green_button_bgd</item>
        <item name="android:textColor">#FFFFFF</item>
    </style> -->
</resources>
